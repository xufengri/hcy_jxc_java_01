<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.example.mapper.GoodPriceMapper">
    <insert id="insert">
        insert into good_price(good_id,role_id,price)values(#{goodId},#{roleId},#{price})
    </insert>
</mapper>